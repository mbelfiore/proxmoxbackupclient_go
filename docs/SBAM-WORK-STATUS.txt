SBAM PBS AGENT - WORK STATUS
============================

DATA/FASE:
2026-08-21 - REPOSITORY CLEANUP COMPLETATO / FASE 2E DA IMPLEMENTARE SU MASTER.

OBIETTIVO FINALE:
Costruire un client Windows per il backup di server fisici direttamente su Proxmox Backup Server, preservando restore file, Bare Metal Restore e futuro Rapid P2V.
PBS resta responsabile di datastore, chunk, FIDX, deduplica, retention, prune, verify e sync.
SBAM PBS Agent resta responsabile di inventario Windows, VSS, dischi/layout/boot metadata e orchestrazione backup/restore.

REPOSITORY CODICE:
mbelfiore/proxmoxbackupclient_go

WORKFLOW GIT ATTUALE:
- Un solo branch GitHub attivo: master.
- Nessuna PR aperta.
- Nessun branch laterale da usare come stato progetto.
- Codex puo lavorare localmente su un branch workspace chiamato work; quel nome locale NON e stato GitHub.
- Una modifica e considerata parte del progetto solo quando e stata revisionata ed e presente su GitHub master.
- Non creare branch/PR salvo richiesta esplicita.

MASTER CONSOLIDATO:
Il lavoro consolidato fino alla Phase 2D.3B e presente su master.
La base upstream Tizbac rimane nella cronologia del fork e puo essere recuperata dalla storia Git se necessario.

REPOSITORY MEMORIA PROGETTO:
mbelfiore/SBAM-PBS-Agent
- un solo branch attivo: main;
- contiene SBAM-DECISIONS.md, SBAM-IDEAS-RAW.md e SBAM-TODO.md.

STATO FASI CONSOLIDATE:
- Phase 1: PASS - harness FIDX deterministico / ricostruzione byte-per-byte.
- Phase 2A: PASS - PBS HTTP/TLS/fingerprint/error handling fail-closed.
- Phase 2B: PASS - worker lifecycle, cancellation, no false success.
- Phase 2B.1: PASS WITH WARNING - close context-aware dei reader; kernel-level Windows I/O cancellation non garantita.
- Phase 2C: PASS - connessione PBS context-aware.
- Phase 2C.1: PASS - upgrade HTTP/1.1 -> PBS robusto/fail-closed.
- Phase 2D: PASS sintetico/cross-build - modello Windows volume/mount/extents e DiskLayout.
- Phase 2D.1: PASS fail-closed - Dynamic/LDM riconosciuto come unsupported.
- Phase 2D.2: PASS fail-closed - identificatori noti Storage Spaces riconosciuti come unsupported.
- Phase 2D.3A: PASS WITH WARNINGS - runtime probe Windows reale per no-mount Volume GUID.
- Phase 2D.3B: PASS implementation/review/test - supporto production plumbing Volume GUID no-mount consolidato su master.
- Phase 2E: NON ANCORA CONSOLIDATA SU MASTER - coordinated multi-volume VSS snapshot set.

EVIDENZA RUNTIME WINDOWS GIA ESEGUITA:
LAB: BACKPREALTEST, Windows Server 2019.
System Reserved senza mount point:
\\?\Volume{3a886445-0000-0000-0000-100000000000}\

Probe SBAM read-only:
- MOUNT_COUNT=0
- EXTENT_COUNT=1
- Disk0
- IsVolumeSupported(Volume GUID)=true
- exit code=0

Microsoft DiskShadow sullo stesso LAB:
- shadow VSS reale creata usando esattamente il canonical Volume GUID no-mount;
- provider Microsoft Software Shadow Copy provider;
- shadow device creato correttamente;
- Auto_Release cleanup verificato;
- dopo l'uscita, vssadmin list shadows non mostrava shadow residue.

IMPORTANTE:
Questa evidenza prova la capacita Windows/VSS del Volume GUID, ma NON prova ancora il percorso production SBAM/go-vss multi-volume.

COMPORTAMENTO 2D.3B CONSOLIDATO:
- Drive-root (es. C:\) resta sorgente VSS preferita.
- Se MountPaths e vuoto, un canonical Volume GUID root con trailing backslash puo essere usato come fallback VSS.
- Il Volume GUID viene passato invariato al package snapshot.
- GUID malformati falliscono chiusi.
- Directory-only non diventa automaticamente supported.
- Multi-extent resta unsupported/fail-closed.
- Dynamic/LDM resta unsupported/fail-closed.
- Storage Spaces resta unsupported/fail-closed.
- FIDX/PBS protocol/DiskLayout/raw-gap reconstruction non sono stati modificati da 2D.3B.

PROBLEMA APERTO CHE BLOCCA IL PRIMO WHOLE-MACHINE BACKUP:
Un normale disco Windows di sistema puo contenere almeno:
- System Reserved -> canonical Volume GUID senza lettera;
- C:\ -> drive root.

Il codice consolidato su master rifiuta ancora piu sorgenti VSS per evitare snapshot presi in tempi differenti.
Questo fail-closed e corretto, ma blocca il primo machinebackup completo del disco di sistema.

FASE 2E - OBIETTIVO IMMEDIATO:
Implementare UN SOLO VSS SNAPSHOT SET coordinato contenente tutte le sorgenti VSS supportate, almeno:
- System Reserved tramite canonical Volume GUID;
- C:\ tramite drive root.

Tutte le sorgenti devono essere aggiunte allo stesso set prima della creazione delle shadow copy.
Il mapping source -> snapshot deve essere deterministico.
Se una sorgente fallisce, nessuna callback di backup deve partire e il cleanup deve essere fail-closed.

GO-VSS:
La Phase 2E deve usare in modo verificato la dipendenza esistente github.com/st-matskevich/go-vss v0.3.3.
Non aggiornare/forkare automaticamente la dipendenza.
Non simulare il coordinamento chiamando CreateSnapshot separatamente per ogni volume.

PROSSIMO ARTEFATTO DI TEST:
Dopo/insieme alla corretta implementazione 2E, creare un piccolo eseguibile Windows non distruttivo dedicato al LAB, ad esempio:
SBAM-VSS-MultiVolume-Test.exe

Deve fare soltanto:
- aprire una singola sessione VSS;
- creare un unico snapshot set;
- aggiungere System Reserved Volume GUID + C:\;
- creare le due shadow nello stesso set;
- mostrare Snapshot Set ID, source, Shadow ID e DeviceObjectPath;
- eseguire cleanup;
- terminare senza backup, senza PBS, senza PhysicalDrive, senza restore.

RUNTIME GATE 2E-R:
Eseguire SBAM-VSS-MultiVolume-Test.exe come amministratore su BACKPREALTEST.
PASS richiede:
- System Reserved e C:\ nello stesso snapshot set reale;
- entrambe le shadow valide;
- cleanup senza shadow residue;
- nessun accesso raw PhysicalDrive;
- nessun backup/restore/PBS.

SOLO DOPO 2E-R PASS:
1. build/use machinebackup.exe;
2. primo BACKPREALTEST -> PBS;
3. restore verificato;
4. poi supporto Dynamic/LDM/RAID necessario per FILESERN;
5. successivamente service/installer/scheduler/GUI.

WARNING APERTI:
- Windows kernel-level pending I/O cancellation non garantita.
- Writer metadata/state e application consistency non production-ready.
- Dynamic/LDM e multi-extent non supportati.
- Storage Spaces reali non supportati.
- newchunk/reusechunk e ChunkUploadStats incompleti.
- alcuni errori caller manifest/blob/finish restano da irrobustire.
- post-upload verification non implementata.
- P2V non production-ready.
- NBD hardening aperto.

STATO FINALE AL 2026-08-21:
Repository Git semplificati: PASS.
2D.3B Volume GUID: CONSOLIDATA SU MASTER.
2E coordinated multi-volume VSS: DA IMPLEMENTARE/REVISIONARE SU MASTER.
2E-R executable runtime test: NEXT.
Windows -> PBS -> restore: PENDING.
PRODUCTION READY: NO.
